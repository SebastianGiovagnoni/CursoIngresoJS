function mostrar()
{
		var contador=0;
		var acumulador=0;
		var respuesta="si";
		var letra;
		var numero;
		var numerosPares;
		var numerosImpares;
		var cantidadCeros;
		var promedioNumerosPositivos;
		var promedioNumerosNegativos;
		var numMaximo;
		var numMinimo;
		var letraMaxima;
		var letraMinima;	

	while(respues!="no")
	{
		numero=prompt("ingrese un numero");
		numero=parseInt(numero);
		while(isNaN(numero) == true || numero<-100 || numero >100 )
		{
			nota=prompt("error, ingrese numero");
			numero=parseInt(numero);
		}
		letra=prompt("ingrese su letra")
		while(isNaN(letra) == false)

	}

}
